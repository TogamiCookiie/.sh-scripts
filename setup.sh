# file is to remain outside of a folder, unless you're wanting to make a sub-folder

mkdir ProjectName # for normal setup, set the name to lowercase so that npm init runs properly
cd ProjectName # update name accordingly to the mkdir cmd
npm init -y
